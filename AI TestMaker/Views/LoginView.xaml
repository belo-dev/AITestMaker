<UserControl x:Class="AI_TestMaker.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#0B1020">

    <UserControl.LayoutTransform>
        <ScaleTransform x:Name="LocalZoom" ScaleX="1" ScaleY="1"/>
    </UserControl.LayoutTransform>

    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">

        <Border Background="#141B2F"
                Padding="30"
                CornerRadius="12"
                Width="380">

            <StackPanel>

                <TextBlock Text="Iniciar sesión"
                           FontSize="30"
                           FontWeight="Bold"
                           Foreground="#5FA8FF"
                           HorizontalAlignment="Center"
                           Margin="0 0 0 25"/>

                <!-- Usuario -->
                <TextBlock Text="Usuario"
                           Foreground="#B0C4DE"
                           Margin="0 0 0 4"/>
                <TextBox x:Name="TxtUser"
                         Height="35"
                         Background="White"
                         Foreground="Black"
                         BorderBrush="#3A7BD5"
                         BorderThickness="1"
                         Padding="6"
                         Margin="0 0 0 12"/>

                <!-- Contraseña -->
                <TextBlock Text="Contraseña"
                           Foreground="#B0C4DE"
                           Margin="0 0 0 4"/>
                <PasswordBox x:Name="TxtPass"
                             Height="35"
                             Background="White"
                             Foreground="Black"
                             BorderBrush="#3A7BD5"
                             BorderThickness="1"
                             Padding="6"
                             Margin="0 0 0 20"/>

                <!-- Entrar -->
                <Button Content="Entrar"
                        Height="40"
                        Background="#3A7BD5"
                        Foreground="White"
                        FontWeight="SemiBold"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="Entrar_Click"/>

                <!-- Crear cuenta -->
                <Button Content="Crear cuenta"
                        Height="40"
                        Background="#5FA8FF"
                        Foreground="White"
                        FontWeight="SemiBold"
                        BorderThickness="0"
                        Cursor="Hand"
                        Margin="0 10 0 0"
                        Click="CrearCuenta_Click"/>

                <!-- Invitado -->
                <Button Content="Entrar como invitado"
                        Height="40"
                        Background="#3A7BD5"
                        Foreground="White"
                        FontWeight="SemiBold"
                        BorderThickness="0"
                        Cursor="Hand"
                        Margin="0 10 0 0"
                        Click="Invitado_Click"/>

            </StackPanel>

        </Border>

    </Grid>

</UserControl>
